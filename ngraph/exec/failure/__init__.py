"""Failure analysis manager package.

Provides `FailureManager` and helpers to run Monte Carlo failure analyses over a
`Network`. Submodules cover pattern enumeration, result aggregation, and
execution helpers. See `manager.py` for the main implementation.
"""

from __future__ import annotations
